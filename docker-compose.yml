version: "3"
services:
    mongodb:
        image: "mongo:3.6"
    redis:
        image: "redis:6"
    server-gin:
        build:
            dockerfile: Dockerfile.dev
            context: ./server-gin
        volumes:
            - ./server-gin:/go/src/app
